apiVersion: v1
kind: Service
metadata:
  name: {{ include "spotify-clone.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "spotify-clone.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 80
